#include <bits/stdc++.h>
using namespace std ;

int main() {
    int t ;
    while(scanf("%d",&t) != EOF) {
        if(t == 42)
        {
            break;
        }
        cout << t << endl;
    }
    return 0 ;
}
